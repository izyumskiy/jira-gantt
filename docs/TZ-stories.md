# ТЗ: ракурс «Эпик — история», проекты и заметки в комментариях Jira

Плагин Jira OhMyGant, версия на момент ТЗ — 1.12.0.

## 1. Цель

Менеджеры отчитываются о статусе в Excel: строка на проект, внутри эпики и пользовательские
истории (User Story) со статусами, заметки рядом. Плагин показывает эпики и задачи, но не
показывает истории и не умеет группировать эпики в проекты. Поэтому таблица живёт отдельно
и статусы в неё переписываются вручную.

Нужно, чтобы эта таблица собиралась в плагине сама:
- **проект → эпик → история** с приоритетами и статусами из Jira;
- диаграмма Ганта по историям — такая же, как на других ракурсах;
- заметки менеджера к эпику и к истории, которые хранятся в комментариях Jira и видны всем.

## 2. Принятые решения

| Вопрос | Решение |
|---|---|
| Что такое проект | Фиктивная сущность: в Jira её нет. Эпики произвольно объединяются в проекты |
| Где хранится принадлежность эпика к проекту | В комментарии к эпику: `(omg project) Название проекта`. Действует последний такой комментарий |
| Можно ли сменить проект из плагина | Да. Плагин публикует новый комментарий `(omg project)` в эпик |
| Уровни заметок | Эпик и история. У проекта заметок нет |
| Где хранятся заметки | В комментариях Jira с кодовым словом `(omg comment)`. Заметка относится только к той задаче, в которой стоит комментарий: в эпике — к эпику, в истории — к истории |
| Как задачи связаны с историями | Связью Jira «relates to» |
| Задачи истории из других эпиков | Догружаются по связи и показываются в истории и в её итоге, но не в итогах эпика и проекта: те считаются по Epic Link (Р5) |
| Приоритет | Поле «Приоритет» Jira. Выводятся пиктограммы приоритетов, проекты сортируются по приоритету |
| Шкала времени | Та же, что на других ракурсах: секции спринтов, «Бэклог», задачи вне спринта (Д1). Недели месяца не вводим |
| Ручные вехи («Публикация», «Полная готовность») | Не делаем |
| Истории и прогноз | Истории показываются как структура, но по-прежнему не считаются задачами в количестве и прогнозе («Типы задач, не учитываемые в подсчётах») |
| Истории на «По эпикам» | Не считаются, как на остальных экранах: итоги эпика на всех ракурсах, в списке, карточке и «Сводке» одинаковые. Загрузка людей историй тоже не учитывает (Р9) |
| Порядок эпиков на «По эпикам» (запрос пользователей) | Можно задать вручную перетаскиванием. Порядок хранится в плагине пользователя, в Jira не пишется (Р8) |
| Веха завершения эпика на диаграммах (запрос пользователей) | Срок исполнения, а если его нет — плановое завершение (Planned End). Срок исполнения важнее. «Сводка» и прогноз по-прежнему считают только от срока исполнения (Р7) |

## 3. Термины

| Термин | Определение |
|---|---|
| Проект | Группа эпиков с общим названием. Живёт только в комментариях `(omg project)` |
| История | Задача Jira типа из настройки «Типы историй» (по умолчанию `User Story`) в выбранном эпике |
| Задача истории | Задача, связанная с историей связью «relates to» (в любую сторону). Эпики и другие истории на том конце связи задачами истории не считаются |
| Своя задача истории | Задача истории с тем же Epic Link, что у истории |
| Чужая задача истории | Задача истории с другим Epic Link: из другого эпика (выбранного или нет) или без эпика |
| Задача эпика | Задача, у которой Epic Link — этот эпик. Итоги эпика считаются только по ним |
| Метка проекта | Комментарий к эпику, который начинается с `(omg project)` |
| Заметка | Комментарий, который начинается с `(omg comment)`. Относится только к той задаче (эпику или истории), в которой опубликован |
| Приоритет проекта | Самый высокий приоритет среди незавершённых эпиков проекта |

Кодовые слова ищутся в начале комментария (после пробелов), без учёта регистра:
`(OMG Project)` и `(omg project)` равнозначны. Комментарии, написанные вручную в Jira в том же
формате, плагин понимает так же, как свои.

Кодовые слова — в **круглых** скобках: квадратные в вики-разметке Jira Server означают ссылку, и
`[omg project]` в самой Jira выглядел бы битой ссылкой. Круглые скобки Jira выводит как обычный
текст.

---

## Функциональные блоки

### Р1. Вкладки-ракурсы: пиктограммы одного стиля

**Назначение.** Три ракурса диаграммы Ганта — одна семья функций. Визуально они должны
отличаться от «Поиска», «Сводки», «Команды» и «Настроек».

- «По эпикам» и «По людям» становятся пиктограммами. Добавляется третья — **«Эпик — история»**.
- Три пиктограммы стоят рядом **общей группой**: одна подложка, один цвет, разделитель
  с соседними вкладками. Активная — подсвечена внутри группы.
- Пиктограммы (единый набор, один стиль линий):

| Ракурс | Пиктограмма | Подсказка |
|---|---|---|
| По эпикам | Горизонтальные полосы диаграммы Ганта | «По эпикам» |
| По людям | Силуэт человека с полосами | «По людям» |
| Эпик — история | Дерево из трёх уровней с полосами | «Эпик — история» |

- Подпись — во всплывающей подсказке и в `aria-label`, переводится вместе с языком.
- «Команда» остаётся текстовой, «Поиск», «Сводка», «Настройки» — как сейчас.

**Приёмка:** три пиктограммы в одной группе, по стилю отличаются от остальных вкладок; каждая
открывает свой ракурс; подсказки на русском и английском.

### Р2. Данные: приоритеты, связи, комментарии

**Новые данные при синхронизации:**

| Что | Откуда | Для чего |
|---|---|---|
| Приоритет эпиков, историй и задач | Поле `priority`: название, id, адрес пиктограммы | Р6 |
| Порядок приоритетов | `/rest/api/2/priority` (порядок в ответе — от высшего к низшему) | Сортировка |
| Связи задач | Поле `issuelinks` | Р5: задачи истории |
| Комментарии эпиков и историй | Поле `comment` отдельным лёгким запросом только по эпикам и историям | Р3, Р4 |
| Чужие задачи историй | Запрос `key in (…)` по ключам из связей историй, которых нет среди задач выбранных эпиков, пачками по 50 | Р5 |

- Комментарии задач не запрашиваются: их много, а кодовые слова там не нужны.
- Комментарии запрашиваются по эпикам и историям, изменившимся с прошлой синхронизации; при
  «Скачать заново» — по всем.
- Чужие задачи историй при каждой синхронизации перечитываются **целиком**, как «Прочие»:
  инкрементальный запрос по Epic Link выбранных эпиков их изменений не видит. Они хранятся
  отдельно от задач эпиков и не попадают в «Сводку», прогноз, «По эпикам», «По людям» и
  «Команду». Если загрузка не удалась, основная синхронизация всё равно завершается, ошибка
  показывается отдельно.
- Если Jira отдала не все комментарии задачи (`total` больше числа полученных), догружаются
  остальные через `/rest/api/2/issue/{key}/comment`.
- Из комментариев сохраняются только разобранные метки проекта и заметки. Остальные комментарии
  в базу не пишутся.
- Новый комментарий меняет поле «Обновлено» у задачи, поэтому «Обновить» подхватывает новые
  метки и заметки без полной перезагрузки.
- Всё пишется в общей транзакции синхронизации (А2). Новые поля — повышение `ISSUE_SCHEMA`,
  одна автоматическая полная перезагрузка.

**Настройки** (карточка «Поля»):

| Настройка | По умолчанию |
|---|---|
| Типы историй | `User Story` (через запятую) |
| Связь задачи с историей | `Relates` (название типа связи Jira; направление не важно) |
| Заметка устарела через, дней | 14 (Р4) |

Типы историй **всегда** исключаются из подсчётов, даже если их нет в «Типах задач, не
учитываемых в подсчётах»: история — контейнер, её нельзя считать задачей и одновременно
историей.

**Приёмка:** после «Обновить» у историй есть приоритет, связанные задачи и разобранные заметки;
комментарий, добавленный в Jira вручную, появляется после следующего «Обновить».

### Р3. Проекты

**Разбор.** Для каждого эпика берётся **последний по дате создания** комментарий `(omg project)`
(правка комментария в Jira его место в очереди не меняет).
Название проекта — текст после кодового слова в первой строке, без пробелов по краям.

| Случай | Результат |
|---|---|
| Меток нет | Эпик в группе «Без проекта» |
| Последняя метка с пустым названием | Эпик выведен из проекта → «Без проекта» |
| Названия отличаются регистром или пробелами | Один проект: сравнение без учёта регистра и лишних пробелов; показывается написание из самой свежей метки |

**Смена проекта из плагина.** На строке эпика — кнопка «Проект…»:
- выпадающий список существующих проектов, поле «Новый проект» и пункт «Без проекта»;
- после выбора плагин публикует в эпик комментарий `(omg project) Название` (для «Без
  проекта» — `(omg project)` без названия) и сразу переносит эпик локально;
- при ошибке публикации эпик остаётся на месте, сообщение — в строке статуса.

**Переименование проекта.** На строке проекта — «Переименовать…». Плагин публикует новую метку
во все эпики проекта **из своей выборки**, включая скрытые галочкой и фильтрами. Перед этим —
подтверждение с числом эпиков: каждый комментарий уходит письмом наблюдателям эпика. В
подтверждении — предупреждение: эпики проекта, которых нет в выборке этого пользователя
(их выбрали другие менеджеры), плагин не видит и не переименует; у них проект останется
со старым названием.

**Если публикация частично не удалась** (нет прав на часть эпиков): переименованные эпики
переходят в новый проект, остальные остаются в старом, в строке статуса — список ключей с
ошибкой.

**Приёмка:**
- эпик с двумя метками попадает в проект из последней;
- смена проекта из плагина видна в Jira комментарием и сразу — на диаграмме;
- «Проект» и «ПРОЕКТ » — один проект.

### Р4. Заметки `(omg comment)`

**Формат.** Первая строка — `(omg comment)`, дальше свободный текст. Автор и дата берутся из
комментария. Действует последняя заметка; предыдущие — история.

**Принадлежность.** Заметка относится **только к той задаче, в которой стоит комментарий**:
- комментарий `(omg comment)` в эпике — заметка эпика, выводится у эпика;
- комментарий `(omg comment)` в истории — заметка истории, выводится у истории;
- у проекта заметок нет: проект — фиктивная сущность, у него нет своей задачи в Jira.
  Заметки его эпиков в проект не поднимаются и не объединяются;
- `(omg comment)` в обычных задачах не разбираются: их комментарии плагин не запрашивает (Р2);
- при переносе эпика в другой проект его заметки остаются с эпиком.

**Отображение** (на ракурсе «Эпик — история»):
- **строка заметки** под строкой эпика и под строкой истории: первые две строки текста,
  автор, дата, полный текст при наведении;
- кнопки ✎ «Изменить» и «История (N)»;
- нет заметки — бледное «Заметки нет · добавить»;
- заметка старше 14 дней (настройка «Заметка устарела через, дней», Р2) помечается «заметке N дн.»;
- галочка «Заметки» на панели ракурса скрывает и показывает строки заметок.

**Правка.** ✎ открывает форму, заполненную текстом последней заметки. «Сохранить в Jira»
публикует новый комментарий `(omg comment)` в эту же задачу (эпик или историю), старая заметка
остаётся в истории. Под кнопкой — предупреждение, что наблюдатели получат письмо.

**Приёмка:**
- заметка, добавленная в плагине к эпику или истории, видна в Jira комментарием в этой же
  задаче, а в плагине — сразу;
- `(omg comment)`, написанный вручную в эпике, выводится только у этого эпика, в истории —
  только у этой истории;
- у строки проекта строки заметки нет.

### Р5. Ракурс «Эпик — история»

**Структура строк:**

```
▾ Проект        [приоритет] Название · эпиков N · историй M · итог по секциям
  ▾ Эпик        [приоритет] Ключ · Epic Name · статус · кнопка «Проект…» · 💬
      заметка эпика
    ▾ История   [приоритет] Ключ · название · статус · полосы по секциям
        заметка истории
      Задачи вне историй — одной строкой «Без истории»
▾ Без проекта
```

**Полосы — та же механика Ганта, что на других ракурсах:**
- секции спринтов, колонка «Бэклог», задачи вне спринта в работе — в текущей секции (Д1);
- полоса истории — её задачи (свои и чужие) по спринтам, с долей готового; клик — список задач;
- если у истории нет задач, но у неё самой есть спринт, — полоса по спринту самой истории, без
  оценки и без доли готового (история — не задача);
- у эпика — итог по задачам эпика (по Epic Link), веха — по правилу Р7;
- у проекта — итог по его эпикам, веха — ближайшая из вех (Р7) его незавершённых эпиков; если
  все эпики завершены — самая поздняя;
- 💬 комментарии эпика, подсказки, свёрнутость по умолчанию и «Развернуть всё» / «Свернуть всё»
  — как на «По эпикам».

**Чужие задачи истории** (задачи из других эпиков или без эпика, связанные с историей):
- показываются в полосе, подсказке и списке задач истории с пометкой «из эпика KEY» или «без
  эпика»; в подсказке истории — сколько задач из них чужих;
- считаются в итоге **истории**: количество, оценка, доля готового — история показывает всю свою
  работу;
- **не** считаются в итогах эпика и проекта: те считаются только по задачам эпика, поэтому
  совпадают с «Сохранёнными эпиками», карточкой эпика и прогнозом. Следствие: сумма итогов
  историй эпика может быть больше итога эпика — это ожидаемо;
- задача выбранного эпика B, связанная с историей эпика A, в итогах B считается как обычно. На
  ветке B она стоит в своей истории, а если своей истории в B у неё нет — в «Без истории»;
- чужая задача, которую не удалось загрузить (нет прав, удалена), в полосу не входит; её ключ —
  в подсказке истории с пометкой «не загружена».

**Сортировка:**
- проекты — по приоритету проекта, затем по названию; «Без проекта» — внизу; проект, у которого
  все эпики завершены, — по самому высокому приоритету среди всех его эпиков;
- эпики в проекте — по приоритету, затем по статусу (как на «По эпикам»), затем по ключу;
- истории в эпике — по приоритету, затем по ключу (по номеру).

**Подсчёты:**
- задача, связанная с несколькими историями, показывается в каждой из них, но в итогах эпика
  и проекта считается один раз;
- «Без истории» — задачи эпика, у которых нет ни одной истории **в этом эпике**;
- сами истории в итогах не считаются; отменённые задачи — как везде: в количестве участвуют,
  в оценке нет.

**Какие эпики:** выбранные на «Поиске» (со снятой галочкой — скрыты), с фильтрами «Поиска» и
фильтром по исполнителю эпика — как на «По эпикам».

**Приёмка:**
- проекты, эпики и истории идут в порядке приоритетов, у каждой строки — пиктограмма приоритета;
- полоса истории строится по связанным задачам; задача без связи — в «Без истории»;
- задача, связанная с двумя историями, видна в обеих, а в итоге эпика — один раз;
- задача невыбранного эпика, связанная с историей, видна в истории с пометкой эпика, входит в
  итог истории и не входит в итог эпика; итог эпика совпадает с «Сохранёнными эпиками».

### Р6. Приоритеты Jira

- У проекта, эпика и истории перед названием — **пиктограмма приоритета** из Jira, название
  приоритета — в подсказке.
- Порядок приоритетов — из Jira (Р2). Нет приоритета — ниже самого низкого.
- Если пиктограммы Jira не загружаются (закрыты для расширения), вместо них — цветной кружок
  по месту в порядке приоритетов (от красного к серому) с названием в подсказке.

**Приёмка:** пиктограммы совпадают с Jira; при недоступных пиктограммах видны цветные кружки.

---

### Р7. Веха завершения эпика: срок исполнения или плановое завершение

**Запрос пользователей.** На «По эпикам» завершение эпика не видно, если у него заполнено только
плановое завершение (Planned End), а срок исполнения (Due date) пуст: веха рисуется только по
сроку исполнения.

**Правило.** Дата вехи эпика на диаграммах:

| Срок исполнения | Плановое завершение | Веха |
|---|---|---|
| Есть | Любое | По сроку исполнения — он важнее |
| Нет | Есть | По плановому завершению |
| Нет | Нет | Вехи нет |

**Отображение:**
- веха по плановому завершению отличается от вехи по сроку исполнения: другой значок на флажке
  (например, «◇» вместо флажка) и подсказка «Плановое завершение: ДД.ММ.ГГ» вместо «Срок
  исполнения»;
- правила цвета те же: срок прошёл или ближе 14 дней — красный, иначе серый, эпик готов — зелёный;
- если заполнены обе даты и они различаются, в подсказке вехи — обе: «Срок исполнения: … ·
  плановое завершение: …».

**Где действует:**
- «По эпикам» — строка эпика и вложенные строки (как сейчас веха срока исполнения);
- «Эпик — история» (Р5) — строки эпиков; веха проекта — ближайшая из вех его эпиков;
- ⚡ критический путь: проверка «не успевает к сроку» считает ёмкость до той же даты вехи.

**Где не действует:** «Сводка», шанс успеть, запас до срока и сигналы опоздания по-прежнему
считаются **только от срока исполнения** — это обещание заказчику (решение ТЗ «Сводка»). Эпик
только с плановым завершением там остаётся «без срока».

**Приёмка:**
- эпик только с плановым завершением получает веху на «По эпикам» со значком плановой даты;
- эпик с обеими датами — веха по сроку исполнения, в подсказке обе даты;
- в «Сводке» эпик только с плановым завершением по-прежнему «без срока».

### Р8. Ручной порядок эпиков на «По эпикам»

**Запрос пользователей.** Эпики на «По эпикам» сортируются автоматически: сверху «в работе» и
«бизнес-тест», ниже «сделать» и «new», внизу готовые, внутри — по объёму. Менеджеру нужен свой
порядок: как строки в его таблице.

**Перетаскивание:**
- у строки эпика в колонке названий — ручка ⋮⋮ слева от номера; эпик тащится за неё;
- во время перетаскивания — линия-указатель между строками эпиков, куда он встанет;
- бросить можно только между эпиками, не внутрь них: вложенные строки исполнителей едут вместе
  со своим эпиком;
- работает и в свёрнутом, и в развёрнутом виде; раскрытость эпика после переноса сохраняется;
- если между видимыми эпиками стоят скрытые (галочкой или фильтром), брошенный эпик встаёт сразу
  после видимого эпика над линией-указателем, перед скрытыми;
- без мыши: ручка получает фокус, **Alt+↑ / Alt+↓** двигают эпик на одну позицию среди видимых;
- номера эпиков («1.», «2.» …) пересчитываются по новому порядку.

**Ручной и автоматический порядок:**
- пока пользователь ничего не перетаскивал — автоматический порядок, как сейчас;
- после первого перетаскивания действует ручной порядок; над диаграммой — отметка «Порядок:
  вручную» и кнопка **«Сбросить порядок»**, которая возвращает автоматический;
- эпик, которого нет в ручном порядке (только что добавлен на «Поиске»), встаёт в конец списка,
  среди таких новых — в автоматическом порядке;
- эпик, убранный из выбора, выпадает из порядка; вернули — снова в конец;
- фильтры и снятые галочки порядок не меняют: скрытый эпик сохраняет своё место и при показе
  встаёт туда же.

**Хранение:**
- порядок хранится в плагине (IndexedDB, служебные ключи), по списку ключей эпиков;
- в Jira не пишется: поле ранга Jira задаёт порядок бэклога на досках команд, и перетаскивание
  менеджера меняло бы его всем;
- попадает в экспорт и импорт конфигурации (`epicOrder`), чтобы порядок можно было передать
  коллеге;
- две открытые вкладки плагина видят один порядок (после перерисовки).

**Где действует:** только «По эпикам». На «Эпик — история» (Р5) эпики внутри проекта по-прежнему
сортируются по приоритету Jira. «По людям», «Поиск» и «Сводка» не меняются.

**Приёмка:**
- перетащенный эпик остаётся на новом месте после перезагрузки страницы и после «Обновить»;
- новый эпик встаёт в конец; «Сбросить порядок» возвращает автоматическую сортировку;
- Alt+↑ / Alt+↓ двигают эпик без мыши;
- порядок переносится экспортом и импортом конфигурации.

### Р9. Истории не считаются на «По эпикам»

**Проблема.** Сейчас «Сохранённые эпики», карточка эпика, прогноз и «Сводка» не считают задачи
исключённых типов (по умолчанию `User Story`), а диаграммы «По эпикам» и «По людям» считают все
задачи. Если у историй есть оценки, работа считается дважды: в истории и в её задачах. Поэтому
итог эпика на «По эпикам» расходится с остальными экранами и с новым ракурсом «Эпик — история» (Р5).

**Какие типы исключаются.** «Типы задач, не учитываемые в подсчётах»; после Р2 к ним всегда
добавляются «Типы историй». Один и тот же набор — на всех экранах.

**Что меняется на «По эпикам».** Задачи исключённых типов не участвуют:
- в полосах эпика и вложенных строк исполнителей: количество, оценка, доля готового;
- в жёлтом баллоне «осталось задач»;
- в подсказке эпика и исполнителя: всего, готово, прочие, без спринта, оценка, по проектам.
  JQL-ссылки из подсказки дополняются условием `AND issuetype not in (…)`, чтобы число в
  подсказке и выборка в Jira по-прежнему совпадали;
- в отборе исполнителей под эпиком: человек, у которого в эпике только истории, строкой не
  выводится;
- в задачах вне спринта, находящихся в работе (Д1);
- в ⚡ критическом пути: конец работ и остаток человека считаются без историй.

**Загрузка людей.** Сейчас загрузка человека по секциям считается по всем его задачам, включая
истории. Она питает окно ⇄ «кто может подменить» и сигналы «Сводки». Исключённые типы из неё
тоже убираются, по той же причине: оценка истории дублирует оценки её задач.

**«Сводка».** Счётчики эпиков, прогноз, шанс, запас, история, «задачи без исполнителя», «задачи
у уволенных», «много задач без оценки» уже считаются без исключённых типов — там ничего не
меняется. Меняется то, что зависит от загрузки людей:
- «перегруз человека» — проценты станут ниже у тех, на ком висят истории с оценками;
- «простой команды рядом с опаздывающим эпиком» — проценты загрузки команды тоже.

Эти сигналы по истории не сравниваются, поэтому пересчитывать прошлые записи не нужно.
Отметки «Принято» у таких сигналов остаются: сигнал вернётся, только если ухудшится сверх порога.
После Р2 в набор исключённых добавляются «Типы историй». Если они отличаются от «Типов задач, не
учитываемых в подсчётах», прогноз и счётчики «Сводки» изменятся на первой синхронизации;
сдвиг попадёт в «Что изменилось» как обычное изменение. Прошлые записи истории не
пересчитываются.

**Что не меняется:**
- «По людям» по-прежнему показывает все задачи человека, включая истории: это список работы, а не
  итог эпика. Загрузка в окне ⇄ считается без историй;
- «Списано» включает время, списанное на истории, — это реально потраченные часы.

**Приёмка:**
- у эпика с историями итог на «По эпикам» совпадает с «Сохранёнными эпиками», карточкой и «Сводкой»;
- число в подсказке совпадает с результатом JQL-ссылки в Jira;
- исполнитель, у которого в эпике только история, строкой под эпиком не выводится;
- загрузка в окне ⇄ и сигнал «перегруз» не учитывают оценки историй.

---

## 4. Нефункциональные требования

- **Приватность.** Плагин пишет в Jira только по явному действию пользователя: смена проекта,
  переименование, заметка и, как раньше, комментарий к эпику из окна 💬. Больше в Jira ничего
  не отправляется.
- **Уведомления.** Каждый комментарий `(omg …)` Jira рассылает наблюдателям задачи. Форма заметки,
  смена проекта и переименование об этом предупреждают.
- **Права.** Для записи нужно право «Добавлять комментарии» в проекте Jira. Нет права — понятное
  сообщение, данные в плагине не меняются.
- **Производительность.** Запрос комментариев — только по эпикам и историям, пачками по 50.
  Построение ракурса — не дольше «По эпикам» на тех же данных.
- **Архитектура.** Разбор кодовых слов, группировка в проекты и итоги — в модуле расчётов без
  интерфейса (как А1), чтобы их проверял селфтест.
- **Язык.** Все тексты — в словаре ru/en. Кодовые слова `(omg project)` и `(omg comment)`
  не переводятся.
- **Тесты.** Селфтест:
  - веха по сроку исполнения / плановому завершению / без обеих дат (Р7);
  - ручной порядок эпиков — перенос, новые эпики в конец, сброс, скрытые эпики на своих местах,
    бросок между видимыми эпиками при скрытых между ними (Р8);
  - разбор меток (последняя, пустая, регистр); заметка относится только к своей задаче
    (эпик / история);
  - задачи в нескольких историях; «Без истории»; эпик и история на том конце связи — не задачи
    истории; тип истории исключён из подсчётов, даже если его нет в исключённых типах;
  - чужие задачи истории: в итоге истории есть, в итоге эпика нет; задача выбранного эпика B
    в истории эпика A — в итоге B один раз;
  - сортировка по приоритетам, «Без проекта»;
  - Р9: модель «По эпикам» и загрузка людей без исключённых типов; JQL подсказки с
    `issuetype not in (…)`; исполнитель только с историями не выводится.
- **Документация.** README, INSTALL и инструкция для менеджеров.

## 5. Вне рамок

- Ручные вехи без задачи в Jira («Публикация», «Полная готовность»).
- Шкала по неделям месяца.
- Заметки на уровне проекта и обычной задачи.
- Удаление и правка опубликованных комментариев: только новый комментарий.
- Проекты и заметки на «По эпикам», «По людям» и в «Сводке».

## 6. Порядок работ

| Этап | Блоки | Результат |
|---|---|---|
| 0 | Р7 | Веха завершения по плановому завершению, если нет срока исполнения. Независим, можно выпустить сразу |
| 0 | Р8 | Ручной порядок эпиков на «По эпикам» перетаскиванием. Независим, можно выпустить сразу |
| 0 | Р9 | Истории не считаются на «По эпикам» и в загрузке людей. Независим: до Р2 работает по «Типам задач, не учитываемых в подсчётах» |
| 1 | Р1 | Пиктограммы ракурсов одной группой |
| 2 | Р2, Р6 | Приоритеты, связи, комментарии в данных; пиктограммы приоритетов |
| 3 | Р3, Р5 | Ракурс «Эпик — история» с проектами, сортировкой и Гантом |
| 4 | Р4 | Заметки эпика и истории: показ, история, правка |

Каждый этап — отдельный релиз с повышением версии.

## 7. Открытые вопросы

Нет.

### Закрытые вопросы

| Вопрос | Решение |
|---|---|
| Цвет оценки в заметке (зелёный / жёлтый / красный) | Не нужен. Заметка — только текст |
| Общий порядок эпиков для всех менеджеров (Р8) | Не нужен. Порядок личный, передаётся коллеге только экспортом конфигурации |
| Задачи истории из невыбранных эпиков | Догружаются по связи, показываются и считаются в истории, в итогах эпика и проекта не считаются (Р2, Р5) |
| Квадратные скобки в кодовых словах — в Jira это ссылка | Кодовые слова в круглых скобках: `(omg project)`, `(omg comment)` |
| Итог эпика на «По эпикам» и «Эпик — история» расходится | Истории исключаются и на «По эпикам», и из загрузки людей (Р9) |
